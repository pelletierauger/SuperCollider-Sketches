s.options.memSize = 2.pow(16);
s.options.memSize;
Safety(s).disable
Safety(s).enabled;
2.pow(16)

s.reboot;
s.boot;
s.quit;

s.options.memoryLocking
s.options.memoryLocking = true


65536 kilobytes
10


2.pow(20) // ça c'est un megabyte

1 kilobyte = 1000 bytes
1 megabyte = 1048576 bytes (2.pow(20))

8192 /1000 * (2.pow(20))

1048576 / 1000 * 8
2.pow(20) *

s.options.memSize = (2.pow(20)) * 1;